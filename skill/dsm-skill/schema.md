# DSM Database Schema

Auto-generated by `scripts/codegen.py --auto --schema-doc`. Do not edit manually.

## Infrastructure Tables

### `excel_workbook`

| Column | Type | Description |
|--------|------|-------------|
| `id` | INTEGER | Primary key |
| `filename` | TEXT | Original file name |
| `blob` | BLOB | Original file binary (for round-trip export) |

### `excel_sheet`

| Column | Type | Description |
|--------|------|-------------|
| `id` | INTEGER | Primary key |
| `workbook_id` | INTEGER | FK -> `excel_workbook.id` |
| `name` | TEXT | Sheet name |
| `header_row` | INTEGER | Detected header row |

### `excel_cell`

| Column | Type | Description |
|--------|------|-------------|
| `id` | INTEGER | Primary key |
| `sheet_id` | INTEGER | FK -> `excel_sheet.id` |
| `row` | INTEGER | Row number |
| `col` | INTEGER | Column number |
| `raw_value` | TEXT | Cell value (or formula string) |
| `cached_value` | TEXT | Calculated value (data_only mode) |
| `style` | JSON | Cell style (bg_color, font, number_format, ...) |
| `comment` | TEXT | Cell note/comment |

## Domain Tables

### `register` (level2_* sheets)

| Column | Type | Excel Header |
|--------|------|-------------|
| `id` | INTEGER | (primary key) |
| `type` | TEXT | TYPE |
| `indx` | TEXT | INDX |
| `page` | TEXT | PAGE |
| `para` | TEXT | PARA |
| `name` | TEXT | NAME |
| `d7` | TEXT | D7 |
| `d6` | TEXT | D6 |
| `d5` | TEXT | D5 |
| `d4` | TEXT | D4 |
| `d3` | TEXT | D3 |
| `d2` | TEXT | D2 |
| `d1` | TEXT | D1 |
| `d0` | TEXT | D0 |
| `init` | TEXT | INIT |
| `sheet_id` | INTEGER | FK -> `excel_sheet.id` |
| `excel_row` | INTEGER | Source row in Excel |

### `memorymap_entry` (memorymap sheet)

| Column | Type | Excel Header |
|--------|------|-------------|
| `id` | INTEGER | (primary key) |
| `baseaddr` | TEXT | BASEADDR |
| `group` | TEXT | Group |
| `midgroup` | TEXT | midgroup |
| `comment` | TEXT | Comment |
| `special` | TEXT | special |
| `sheet_id` | INTEGER | FK -> `excel_sheet.id` |
| `excel_row` | INTEGER | Source row in Excel |

### `overview_entry` (overview sheet)

| Column | Type | Description |
|--------|------|-------------|
| `id` | INTEGER | Primary key |
| `category` | TEXT | Current section (#header) |
| `key` | TEXT | Key name (# stripped) |
| `value` | TEXT | Value |
| `comment` | TEXT | Column C comment |
| `is_commented` | BOOLEAN | #key with value (commented-out entry) |
| `is_category` | BOOLEAN | Category header row |
| `sheet_id` | INTEGER | FK -> `excel_sheet.id` |
| `excel_row` | INTEGER | Source row in Excel |

## Example SQL Queries

```sql
-- List all register entries
SELECT type, indx, page, para, name, d7, d6, d5, d4, d3, d2, d1, d0, init FROM register ORDER BY excel_row;

-- List all memorymap_entry entries
SELECT baseaddr, group, midgroup, comment, special FROM memorymap_entry ORDER BY excel_row;

-- List overview entries (key-value)
SELECT category, key, value, comment FROM overview_entry ORDER BY excel_row;

-- Count entries per sheet
SELECT s.name AS sheet, COUNT(c.id) AS cells
FROM excel_sheet s LEFT JOIN excel_cell c ON c.sheet_id = s.id
GROUP BY s.id;

-- Search cells by value
SELECT s.name, c.row, c.col, c.raw_value
FROM excel_cell c JOIN excel_sheet s ON c.sheet_id = s.id
WHERE c.raw_value LIKE '%search_term%';
```
